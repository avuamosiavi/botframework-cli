{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "build": "lerna run build && lerna bootstrap --no-ci",
    "test": "lerna run test --no-bail",
    "lernacoverage": "lerna run coverage --no-bail",
    "upload-coveralls": "nyc report --reporter=text-lcov | coveralls"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.2.1"
  }
}
