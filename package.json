{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "build": "lerna run build && lerna bootstrap --no-ci",
    "test": "lerna run test --no-bail --no-ci",
    "lernacoverage": "lerna run testcoverage --no-bail --no-ci",
    "upload-coveralls": "nyc report | coveralls"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.2.1"
  }
}
